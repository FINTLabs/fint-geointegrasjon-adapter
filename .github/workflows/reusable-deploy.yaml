name: Reusable Deploy Workflow
on:
  workflow_call:
    inputs:
      tags: 
        required: true
        type: string
      environment:
        required: true
        type: string
      deploy-every-branch:
        required: false
        type: boolean
        default: false
      org:
        required: false
        type: string
        default: ''

jobs:
  deploy-to-aks:
    name: deploy
    environment: ${{ inputs.environment }}
    if: >-
      github.actor != 'dependabot[bot]' &&
      (
        github.ref == format('refs/heads/{0}', github.event.repository.default_branch) ||
        inputs.deploy-every-branch == true
      )
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Bake manifests with Kustomize
        id: manifest
        uses: azure/k8s-bake@v4
        with:
          renderEngine: kustomize
          kustomizationPath: kustomize/overlays/${{ inputs.org && format('{0}/{1}', inputs.org, inputs.environment) || inputs.environment }}

      - name: Apply image to Kubernetes manifests
        id: substitute
        uses: azure/k8s-artifact-substitute@v2
        with:
          images: ${{ inputs.tags }}
          manifests: ${{ steps.manifest.outputs.manifestsBundle }}

      - name: Login to Azure
        uses: azure/login@v3
        with:
          creds: "${{ secrets[format('AKS_{0}_FINT_GITHUB', inputs.environment)] }}"

      - name: Set the target cluster
        uses: azure/aks-set-context@v5
        with:
          cluster-name: ${{ vars[format('{0}_CLUSTER_NAME', inputs.environment)] }}
          resource-group: rg-aks-${{ inputs.environment }}
          admin: true

      - name: Get namespace from manifest
        id: namespace
        run: |
          namespace=$(yq '.metadata.namespace' ${{ steps.substitute.outputs.manifests }} | head -n 1 | sed 's/^null$/default/')
          echo "name=$namespace" >> $GITHUB_OUTPUT

      - name: Deploy to Kubernetes
        uses: azure/k8s-deploy@v6
        with:
          action: deploy
          manifests: ${{ steps.substitute.outputs.manifests }}
          images: ${{ inputs.tags }}
          namespace: ${{ steps.namespace.outputs.name }}
